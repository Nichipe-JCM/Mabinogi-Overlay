<Window x:Class="TestOverlay.App.ProfileNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save Profile"
        Icon="/Image/AppIcon.ico"
        Width="390"
        Height="190"
        MinWidth="340"
        MinHeight="170"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource OverlayBgBrush}">
    <Border Margin="18" Padding="16" Style="{StaticResource GlassCardStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Profile name" Style="{StaticResource SectionTitleStyle}"/>
            <TextBox Grid.Row="1" x:Name="ProfileNameBox" Height="34" Margin="0,10,0,0"/>
            <TextBlock Grid.Row="2" x:Name="ErrorText" Margin="0,8,0,0" Style="{StaticResource DangerTextStyle}"/>

            <WrapPanel Grid.Row="3" HorizontalAlignment="Right">
                <Button Width="84" Height="34" Content="Cancel" Click="CancelButton_Click"/>
                <Button Width="84" Height="34" Margin="8,0,0,0" Content="Save" Click="SaveButton_Click"/>
            </WrapPanel>
        </Grid>
    </Border>
</Window>

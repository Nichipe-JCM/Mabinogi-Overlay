<Window x:Class="TestOverlay.App.ProfileNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save Profile"
        Width="360"
        Height="170"
        MinWidth="320"
        MinHeight="160"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#F3F5F7">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Profile name" FontWeight="SemiBold" Foreground="#20242B"/>
        <TextBox Grid.Row="1" x:Name="ProfileNameBox" Height="32" Margin="0,8,0,0"/>
        <TextBlock Grid.Row="2" x:Name="ErrorText" Margin="0,8,0,0" Foreground="#B91C1C"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="82" Height="32" Content="Cancel" Click="CancelButton_Click"/>
            <Button Width="82" Height="32" Margin="8,0,0,0" Content="Save" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>

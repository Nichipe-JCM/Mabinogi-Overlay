# 프로젝트 작업 지침

## 소통과 작업 범위

- 반드시 존댓말을 사용합니다.
- 확인한 사실과 추정, 자동 검증 결과와 실제 실행 검증의 한계를 구분합니다.
- 작업 현황과 결과는 대화에서 간결하게 설명합니다.
- 투두 목록과 작업 계획은 기본적으로 대화에 작성합니다. 별도 요청 없이 이를 위한 Markdown 파일이나 커밋을 만들지 않습니다.
- 사용자가 승인한 작업은 필요한 구현과 검증까지 진행하며, 이미 승인한 일상적인 작업에 재확인을 요구하지 않습니다.

## 프로젝트 경계

- 이 프로젝트는 Windows용 마비노기 퀵슬롯 오버레이이며, 상태 모니터와 에린 시간 알림 기능을 포함합니다.
- 공식 Windows 캡처 API와 독립된 투명 오버레이 창만 사용합니다.
- 게임 메모리 읽기, 코드 주입, 게임 렌더링 후킹, 입력 자동화, 안티치트 우회, 동작 은폐를 추가하지 않습니다.

## 구현 원칙

- 퀵슬롯 자동 감지는 보조 기능입니다. 후보 편집기와 레이아웃 편집기의 사용자 보정을 기준으로 삼습니다.
- 슬롯 크기, 구역 구조, 원본 좌표는 프로필별 캡처 픽셀 데이터로 취급합니다.
- 정상 실행 중 오버레이는 최상위, 클릭 통과, 비활성화 창 동작을 유지합니다.
- 실시간 캡처·렌더링 자원을 재사용합니다. 프레임마다 WGC 세션을 생성하지 않습니다.
- 앱 UI 문자열은 `src/TestOverlay.App/Localization/en-US.lang`과 `ko-KR.lang`에 함께 추가합니다.
- 설정은 로컬의 이식 가능한 데이터로 유지합니다. 개인 인증서, 자격 증명, 로컬 로그, 시험 캡처는 Git에 추가하지 않습니다.
- 기능 작업 전 `docs/CODEX_PROJECT_HANDOFF.md`를 읽되, 작업 규칙은 이 파일을 기준으로 합니다.

## 검증

- 변경에 맞는 빌드와 자동 테스트를 수행합니다. 실행 중인 앱과 충돌하지 않도록 별도 출력 디렉터리를 사용합니다.
- GUI를 띄우지 않는 단위 테스트, 합성 데이터 성능 측정, 격리된 저장·프로세스 통합 검증은 자율적으로 수행할 수 있습니다.
- 시험 데이터와 오류 주입은 실제 사용자 설정에서 격리합니다.
- 실제 앱 실행, GUI 조작, 게임 캡처·감지, 데스크톱 검사는 사용자가 명시적으로 요청한 경우에만 수행합니다. 실제 게임 환경 검증은 기본적으로 사용자가 담당합니다.
- 자동 테스트 통과를 실제 게임·GPU·다중 모니터 환경 검증 완료로 보고하지 않습니다.

## Git 작업

- 수정 전 `git status --short --branch`와 최근 커밋을 확인하고, 기존 사용자 변경을 보존합니다.
- 별도 브랜치가 필요한 경우 목적에 맞게 생성하며, 사용자 지정이 없으면 `codex/` 접두사를 사용합니다.
- 검증을 마친 변경은 기능별로 구분하여 커밋합니다.
- **커밋 후에는 해당 작업 브랜치의 원격 브랜치로 푸시하는 것이 기본 원칙입니다. 별도 푸시 요청이나 재확인을 기다리지 않습니다.** 사용자가 푸시 보류를 지시한 경우에는 그 지시를 따릅니다.
- 푸시가 거절되면 원인을 확인하고 보고합니다. 기본 푸시 승인을 강제 푸시, 원격 기록 삭제, 브랜치 삭제 승인으로 해석하지 않습니다.
- 커밋·푸시 후 결과와 작업 트리 상태를 확인합니다. 로컬 커밋과 원격 반영 여부를 구분하여 보고합니다.
- 병합, 릴리스, 배포는 사용자가 요청한 범위에서만 수행합니다.
